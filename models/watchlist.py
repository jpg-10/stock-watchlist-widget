class watchlist():
    def __init__(self, name, products, sort_field, sort_direction):
        self.name = name
        self.products = products
        self.sort_field = sort_field
        self.sort_direction = sort_direction